// import * as c from './../actions/ActionTypes';

export default (state = {}, action) => {
  // const { project, skill, bio, id } = action; // destructuring, testing to see if firestore needs action to be saved in const
  switch (action.type) {
  default:
    return state;
  }
};